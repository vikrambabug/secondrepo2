pipeline {
    agent any
    stages {
        stage('stage1') {
           steps {
                echo "Hi This is vikram"
                sh 'mkdir firstdir3'
                sh 'pwd'
                sh 'ls -lrt'
               sh 'date'
               sh 'whoami'
            }
}
        stage('stage2') {
            steps{
            echo 'this is stage2'
}
}
    }
}
